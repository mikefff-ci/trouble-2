version: 2.1

parameters:
  testing:
    type: string
    default: unchanged

jobs:
  testdir:
    docker:
      - image: cimg/base:stable
    steps:
      - run: |
          export HEDGEHOG_COLOR=0
          mkdir -p /tmp/test
          read
      - run: echo "<< pipeline.parameters.testing >>"
workflows:
  wf:
    jobs:
      - testdir
