version: 2.1

jobs:
  build-test:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo 123

workflows:
  main:
    jobs:
      - build-test:
          context: a
      - build-test:
          context:
            - a
            - b
