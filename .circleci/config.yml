version: 2.1

workflows:
  testing:
    jobs:
      - runner

jobs:
  runner:
    machine: true
    resource_class: "mkly/mkly-one"
    steps:
      - run: sleep 4000
      - run: echo "Hi I'm on Runners!"
