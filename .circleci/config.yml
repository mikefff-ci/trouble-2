version: 2.1

jobs:
  python:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - add_ssh_keys
      - run: cat ~/.ssh/config
      - run: |
          sed -i 's/Host ramp\-util\.github\.com/Host ramp\-util\.github\.com\n  HostName github\.com/g' ~/.ssh/config
      - run: cat ~/.ssh/config
workflows:
  comments:
    jobs:
      - python
