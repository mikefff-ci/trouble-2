version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
    working_directory: /home/circleci/project/

    steps:
      - run: pwd
      - run: echo $CIRCLE_WORKING_DIRECTORY
        #      - run: rmdir $CIRCLE_WORKING_DIRECTORY
      - checkout
workflows:
  version: 2
  test:
    jobs:
      - test
