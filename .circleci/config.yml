version: 2.1

orbs:
  win: circleci/windows@1.0.0

jobs:
  loonix:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          command: ./trap.sh
          background: true
      - run: sleep 60
workflows:
  wf:
    jobs:
      - loonix
