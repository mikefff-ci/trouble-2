version: 2.1

jobs: # a basic unit of work in a run
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo 1
workflows:
  build:
    jobs:
      - test
      - wait:
          type: approval
          requires:
            - test
          
