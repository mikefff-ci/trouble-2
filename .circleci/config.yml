version: 2.1

jobs:
  main:
    docker:
      - image: circleci/python:3.7
      - image: circleci/python:3.8
        command: 'echo $COMMAND'
        environment:
          - $COMMAND
    steps:
      - run: echo 123

workflows:
  workflow:
    jobs:
      - main
