version: 2.1

jobs:
  build:
    docker:
      - image: circleci/golang
        environment:
          IMPORTED: ${MY_TEST_VALUE}
        command: echo $IMPORTED
    steps:
      - run: exit 0

workflows:
  wf:
    jobs:
      - build
