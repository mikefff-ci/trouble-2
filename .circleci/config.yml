version: 2.1

jobs:
  test:
    docker:
      - image: docker:18.05.0-ce-git
    steps:
      - checkout
      - run: apk add tcpdump
      - setup_remote_docker:
          version: 18.05.0-ce
      - run: tcpdump port 443 -w dump.txt
      - run: docker build .
      - store_artifacts:
          path: dump.txt

workflows:
  wf:
    jobs:
      - test:
          name: with/slash
