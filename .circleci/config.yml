version: 2.1

jobs:
  build:
    docker:
      - image: circleci/golang:1.13.8
      - image: circleci/postgres:latest
    steps:
      - run: echo $TEST_VALUE

workflows:
  wf:
    jobs:
      - build
