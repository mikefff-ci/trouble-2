version: 2.1

orbs:
  aws-ecs: circleci/aws-ecs@1.1.0

jobs:
  vm:
    docker:
      - image: cimg/base:stable
    steps:
      - setup_remote_docker:
          version: 19.03.12
      - run: docker system df
workflows:
  wf:
    jobs:
      - vm
