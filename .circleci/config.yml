version: 2.1

jobs:
  test:
    docker:
      - image: circleci/php:7.4
    steps:
      - checkout
      - run: |
          (
            exit 1;
          ) || {
            if [ $? -ne 0 ]; then
              echo 123;
            fi
          }
      - run: |
          cd new-symfony-app
          composer install
          ./bin/phpunit

      - store_test_results:
          path: phpresults
      - store_artifacts:
          path: phpresults
workflows:
  wf:
    jobs:
      - test:
          name: with/slash
