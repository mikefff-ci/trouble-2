version: 2.1

jobs:
  build:
    docker:
      - image: circleci/golang:1.13.8
          environment:
            - IMPORTED: ${TEST_VALUE}
          command: echo $IMPORTED
    steps:
      - run: exit $IMPORTED

workflows:
  wf:
    jobs:
      - build
