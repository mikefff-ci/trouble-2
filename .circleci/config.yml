version: 2.1

jobs:
  test:
    docker:
      - image: docker:18.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker:
          version: 18.05.0-ce
      - run: sudo ip link set dev eth0 mtu 1500
        #      - run:
        #  command: ssh remote-docker sudo tcpdump
        #  background: true
      - run: docker build .
      - store_artifacts:
          path: dump.txt

workflows:
  wf:
    jobs:
      - test:
          name: with/slash
