version: 2.1

jobs:
  main:
    docker:
      - image: circleci/node
    steps:
      - restore_cache:
          key: v3
      - run: |
          if [ ! -d "cachetest" ]; then
            mkdir cachetest
            echo 'test2' > cachetest/test.json
            gzip --keep cachetest/test.json
          fi
          ls cachetest
      - save_cache:
          key: v3
          paths:
            - cachetest

workflows:
  workflow:
    jobs:
      - main
