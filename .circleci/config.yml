version: 2.1

jobs:
  comment:
    docker:
      - image: ${IMAGE_TEST}:stable
    steps:
      - setup_remote_docker
      - run: ssh remote-docker "df | grep '/var/lib/docker' | awk '{print $4}'"

workflows:
  comments:
    jobs:
      - comment
