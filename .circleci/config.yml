version: 2.1

workflows:
  version: 2
  wf:
    jobs:
      - myjob

jobs:
  myjob:
    docker:
      - image: circle/python:3.8
    steps:
      - run: sudo apt-get install -y default-jdk
      - run: which pkill
