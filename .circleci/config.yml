version: 2.1

jobs:
  test:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - run: |
          docker run --rm indigoag/circleci-image-test:3.7-stretch-indigo-development sudo /bin/sh -c 'ls /usr/local/lib/python3.7/site-packages/cryptography/hazmat/primitives/'

workflows:
  wf:
    jobs:
      - test
