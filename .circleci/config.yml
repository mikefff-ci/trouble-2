version: 2.1

jobs:
  trap:
    machine:
      image: ubuntu-1604:202007-01
    steps:
      - checkout
      - git clone https://not:correct@bitbucket.org/humancaresystems/hcs-resilix-pipelines.git/
      - run: echo 123

workflows:
  wf:
    jobs:
      - trap
