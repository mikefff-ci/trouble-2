version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - run: whoami

workflows:
  wf:
    triggers:
      - schedule:
          cron: '*/1 * * * *'
          filters:
            branches:
              only:
                - /.*/
    jobs:
      - test
          
