version: 2.1

jobs:
  wget:
    docker:
      - image: docker:18.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker:
          version: 18.05.0-ce
      - run: docker build -f Dockerfile-wget .
  curl:
    docker:
      - image: docker:18.05.0-ce-git
    steps:
      - checkout
      - setup_remote_docker:
          version: 18.05.0-ce
      - run: docker build -f Dockerfile .

workflows:
  wf:
    jobs:
      - wget
      - curl
