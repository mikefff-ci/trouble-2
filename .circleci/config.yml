version: 2.1

orbs:
  aws-serverless: circleci/aws-serverless@1.0.2

jobs:
  testcache:
    executor: aws-serverless/default
    steps:
      - aws-serverless/install

workflows:
  default-ci-workflow:
    jobs:
      - testcache
