version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - run: echo 1 > /tmp/test.txt
      - store_artifacts:
          path: /tmp/test.txt
workflows:
  version: 2
  build:
    jobs:
      - build:
          filters:
            branches:
              only: /pull\/.*/
