version: 2.1

jobs:
  test:
    docker:
      - image: indigoag/circleci-image-test:3.7-stretch-indigo-development
    steps:
      - run: ls -la /usr/local/lib/python3.7/site-packages/cryptography/hazmat/primitives

workflows:
  wf:
    jobs:
      - test
