version: 2.1

jobs:
  vm:
    docker:
      - image: mkly/curl-gnutls
        auth:
          username: testing
          password: ""
    steps:
      - run: echo "test"

workflows:
  wf:
    jobs:
      - vm
