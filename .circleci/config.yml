version: 2.1

jobs:
  npm-example:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "<< pipeline.git.base_revision >>"
      - run: echo "<< pipeline.git.revision >>"

workflows:
  wf:
    jobs:
      - npm-example
