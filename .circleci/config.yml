version: 2.1

jobs:
  test:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: echo $(($TEST_ONE == "testing"))

workflows:
  workflow:
    jobs:
      - test:
          context: org-test
