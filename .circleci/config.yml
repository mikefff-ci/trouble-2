version: 2.1

jobs:
  main:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: echo $(($TEST_ONE == "testing"))
  forked:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: echo "forked"

workflows:
  workflow:
    jobs:
      - main:
          context: org-test
          filters:
            branches:
              only:
                - master
                - /internal\/.*/
      - forked
