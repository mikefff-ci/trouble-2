version: 2.1

jobs:
  build:
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - run: |
          go get "golang.org/x/crypto/ssh"
      - run: go test

workflows:
  wf:
    jobs:
      - build
