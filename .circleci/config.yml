version: 2.1
orbs:
  aws-cli: circleci/aws-cli@0.1.13

jobs:
  trap:
    docker:
      - image: rotterdamlogisticslab/pronto-orb:master
    steps:
      - aws-cli/install

workflows:
  wf:
    jobs:
      - trap
