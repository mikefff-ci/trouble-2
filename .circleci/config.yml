version: 2.1

orbs:
  win: circleci/windows@2.4.0

jobs:
  test:
    executor:
      name: win/default
      shell: bash.exe
    steps:
      - run: docker run --rm winamd64/golang go

workflows:
  wf:
    jobs:
      - test
