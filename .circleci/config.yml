version: 2.1

jobs:
  vm:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "test"
  gg:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "gg"
workflows:
  wf:
    jobs:
      - gg:
          filters:
            tags:
              only: /.*/
            branches:
              ignore: /.*/
      - hold-for-approval:
          type: approval
          requires:
            - gg
      - vm:
          requires:
            - hold-for-approval
            - gg
