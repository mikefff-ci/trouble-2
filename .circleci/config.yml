version: 2.1

orbs:
  node: circleci/node@4.1.0

jobs:
  npm-example:
    docker:
      - image: circleci/android-29
    steps:
      - checkout
      - node/install:
          install-npm: true
      - run: node --version

workflows:
  wf:
    jobs:
      - npm-example
