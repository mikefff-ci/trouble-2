version: 2.1

jobs:
  vm:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - run: which nope || true
      - run: echo $PATH
      - run: ls /usr/local/bin || true
      - run: ls /usr/local/dsofji || true

workflows:
  wf:
    jobs:
      - vm
