version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:table
      - image: adobe/s3mock:latest
        name: s3mock

    steps:
      - run: |
          while ! nc -z localhost 9090 ; do
            sleep 1
          done

          while ! nc -z localhost 9191 ; do
            sleep 1
          done

          netstat -plnt

workflows:
  version: 2
  test:
    jobs:
      - test
