version: 2.1

parameters:
  testing:
    type: string
    default: unchanged

jobs:
  testdir:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "<< parameters.testing >>"
workflows:
  wf:
    jobs:
      - testdir
