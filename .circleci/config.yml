version: 2.1

jobs:
  testing:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - run: echo "test"
      - run: docker image ls
      - run: docker build .
      - run: docker image ls

workflows:
  wf:
    jobs:
      - testing
