version: 2.1

workflows:
  testing:
    jobs:
      - testing

jobs:
  testing:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - store_test_results:
          path: /home/circleci/project/tr
