version: 2.1

jobs:
  vm:
    machine:
      image: ubuntu-1604:201903-01
      docker_layer_caching: true
    steps:
      - run:
          background: true
          command: |
            while true ; do
              ssh remote-docker 'sudo df -h'
              sleep 5
            done
      - run: sleep 20
workflows:
  wf:
    jobs:
      - vm
