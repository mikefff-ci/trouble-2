version: 2.1

jobs:
  comment:
    docker:
      - image: ${IMAGE_TEST}:stable
    steps:
      - setup_remote_docker
      - run: sudo df
      - run: ssh remote-docker "sudo df"

workflows:
  comments:
    jobs:
      - comment
