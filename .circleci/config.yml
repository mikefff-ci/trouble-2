version: 2.1

jobs:
  build-test:
    docker:
      - image: cimg/base:stable
    working_directory: /home/circleci/project/src
    steps:
      - checkout

workflows:
  main:
    jobs:
      - build-test
