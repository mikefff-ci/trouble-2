version: 2.1

jobs:
  build:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - run:
          name: Log memory
          background: true
          command: |
            while true ; do
              sleep 2
              ps -o %cpu,%mem,cmd
            done
      - run: |
          echo 123
          sleep 10
          echo 333
workflows:
  wf:
    jobs:
      - build
