version: 2.1

jobs:
  testcache:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: mkdir uuu
      - run: echo "test" > uuu/test.txt
    store_artifacts:
      - path: uuu/

workflows:
  default-ci-workflow:
    jobs:
      - testcache
