version: 2.1

jobs:
  vm:
    docker:
      - image: cimg/base:stable
          auth:
            username: mkly
            password: noooooo
    steps:
      - run: echo "test"
  ttt:
    docker:
      - image: cimg/base:stable

workflows:
  wf:
    jobs:
      - vm
