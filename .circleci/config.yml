version: 2.1

jobs:
  testdir:
    docker:
      - image: cimg/base:stable
    working_directory: /var/www/html
    steps:
      - checkout
      - run: echo 123
      - persist_to_workspace:
          root: /var/www/html
          paths:
            - .
workflows:
  wf:
    jobs:
      - testdir
