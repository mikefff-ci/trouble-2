version: 2.1

jobs:
  vm:
    docker:
      - image: cimg/base:stable
        auth:
          username: mkly
          password: ""
    steps:
      - run: echo "test"

workflows:
  wf:
    jobs:
      - vm
