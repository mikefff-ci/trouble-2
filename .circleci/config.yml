version: 2.1

jobs:
  sigh:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - run: |
         circleci tests split --total=92 ./empty_list_of_files.txt 

workflows:
  wf:
    jobs:
      - sigh
