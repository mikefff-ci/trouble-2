version: 2.1

jobs:
  android:
    docker:
      - image: circleci/android:api-29-node
    steps:
      - run: touch $BASH_ENV
      - run: cat $BASH_ENV
  python:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: touch $BASH_ENV
      - run: cat $BASH_ENV
workflows:
  comments:
    jobs:
      - android
      - python
