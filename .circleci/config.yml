version: 2.1

orbs:
  ts: mkly/test-splitting@dev:test/slashes

jobs:
  build:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: echo 123
workflows:
  wf:
    jobs:
      - build
