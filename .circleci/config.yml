version: 2.1

jobs:
  comment:
    docker:
      - image: cimg/base:stable
    environment:
      SOME_ENV: <<! another comment >>Testing
    steps:
      - run: |
          echo "above"
          <<! We can also do comments >>
          echo "below"

workflows:
  comments:
    jobs:
      - comment
