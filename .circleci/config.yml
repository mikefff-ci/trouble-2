version: 2.1

parameters:
  should:
    type: boolean
    default: false

jobs:
  test:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: echo $(($TEST_ONE == "testing"))
  test2:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: echo "<< pipeline.project.git_url >>"

workflows:
  workflow:
    jobs:
      - test:
          when: << pipelines.parameters.should >>
          context: org-test
      - test2
