version: 2.1

jobs:
  vm:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "test"
  gg:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "gg"
workflows:
  wf:
    jobs:
      - gg:
          name: something-else
      - vm:
          requires:
            - something-else
