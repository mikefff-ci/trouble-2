version: 2.1

jobs:
  trap:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          command: ./trap.sh
          background: true
      - run: sleep 60
workflows:
  wf:
    trap:
      - loonix
