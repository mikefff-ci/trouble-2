version: 2.1

parameters:
  one:
    default: ${PIPELINESSS}
    type: string
jobs:
  test:
    docker:
      - image: circleci/python:2.7.16-stretch
    steps:
      - run:
          name: Name << pipeline.parameters.one >>
          command: echo "this << pipeline.parameters.one >>"

workflows:
  wf:
    jobs:
      - test
