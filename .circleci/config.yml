version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
    working_directory: /home/circleci/project/src

    steps:
      - checkout
workflows:
  version: 2
  test:
    jobs:
      - test
