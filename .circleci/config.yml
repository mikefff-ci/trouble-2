version: 2.1

jobs:
  vm:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - run:
          background: true
          command: |
            while true ; do
              sudo df -h
              sleep 5
            done
      - run: sleep 20
workflows:
  wf:
    jobs:
      - vm
