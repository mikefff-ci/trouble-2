version: 2.1

orbs:
  docker-cpu-profiling: mkly/docker-cpu-profiling@dev:0.1.2

jobs:
  cpu:
    docker:
      - image: circleci/node:13.10
    steps:
      - checkout
      - docker-cpu-profiling/profile
      - run:
          command: CI=false npm install

workflows:
  wf:
    jobs:
      - cpu
