version: 2.1

jobs:
  vm:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "test"
  ttt:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "eweorijwer"

workflows:
  wf:
    jobs:
      - vm
      - ttt
