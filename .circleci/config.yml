version: 2.1

workflows:
  testing:
    jobs:
      - testing

jobs:
  testing:
    docker:
      - image: circleci/node:latest
    resource_class: 2xlarge+
    steps:
      - checkout
      - run: |
          git fetch --all
          git pull --all
      - run: git tag
      - run: git branch
