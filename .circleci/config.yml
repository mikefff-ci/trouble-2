version: 2.1

orbs:
  maven: circleci/maven@1.0.2
executors:
  jdk8:
    docker:
      - image: cimg/openjdk:8.0.265
workflows:
  maven_test:
    jobs:
      - maven/test:  # checkout, build, test, and upload test results
          maven_command: 'mvn -U -X'
          app_src_directory: appf-salesforce-sys
          name: salesforce_test
          executor: jdk8
