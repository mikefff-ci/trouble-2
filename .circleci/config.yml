version: 2.1

jobs:
  vm:
    docker:
      - image: cimg/base:stable
    steps:
      - setup_remote_docker:
          experimental: true
          verion: 19.03.12
      - run: |
          cd docker
          DOCKER_BUILDKIT=1 docker build .
workflows:
  wf:
    jobs:
      - vm
