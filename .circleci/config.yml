version: 2.1

workflows:
  testing:
    jobs:
      - testing

jobs:
  testing:

    docker:
      - image: cimg/base:stable

    steps:
      - run: curl -L https://hqo.okta.com -o /tmp/okta.html
      - store_artifacts:
          path: /tmp/okta.html
