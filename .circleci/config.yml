version: 2.1

parameters:
  thefunc:
    type: string
    default: '(fn [content ctx render] false)'
  yourname:
    type: string
    default: "Mike"
jobs:
  comment:
    docker:
      - image: cimg/base:stable
    environment:
      SOME_ENV: <<! another comment >>Testing
    steps:
      - run: |
          echo "above"
          <<! We can also do comments >>
          echo "below"
          echo $SOME_ENV
          <<#pipeline.parameters.thefunc>>
            echo "<< pipeline.parameters.yourname >>"
          <</pipeline.parameters.thefunc>>
workflows:
  comments:
    jobs:
      - comment
