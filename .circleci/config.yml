version: 2.1

jobs:
  testing:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run: docker volume ls
      - run: docker volume create testcache
      - run: docker volume ls

workflows:
  wf:
    jobs:
      - testing
