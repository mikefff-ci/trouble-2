version: 2.1

jobs:
  testdir:
    docker:
      - image: cimg/base:stable
    working_directory: /home/circleci/project/src
    steps:
      - setup_remote_docker
      - run: docker run --rm -e POSTGRES_PASSWORD=mysecretpassword -d postgres
      - run: |
          while ! nc -z remote-docker 5432; do
            sleep 1
            echo "checking"
          done
workflows:
  wf:
    jobs:
      - testdir
