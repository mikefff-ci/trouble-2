version: 2.1

jobs:
  testing:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - run: echo "test"
      - run: docker volume ls
      - run: docker volume create
      - run: docker build .
      - run: docker volume ls

workflows:
  wf:
    jobs:
      - testing
