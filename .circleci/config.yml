version: 2.1

orbs:
  win: circleci/windows@1.0.0

jobs:
  loonix:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - run: |
          echo '<< pipeline.git.base_revision >>'
workflows:
  wf:
    jobs:
      - loonix
