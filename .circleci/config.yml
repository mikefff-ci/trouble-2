version: 2.1

orbs:
  win: circleci/windows@2.4.0

jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - run: |
          n=0
          until [ "$n" -ge 5 ]
          do
            echo "running"
            curl "asdfads" && break  # substitute your command here
            n=$((n+1)) 
            sleep $(($n * 2))
          done

workflows:
  wf:
    jobs:
      - test
