version: 2.1

jobs:
  sigh:
    docker:
      - image: cimg/base:latest
    steps:
      - checkout
      - run: |
         circleci tests split --total=92 ./list_of_files.txt 

workflows:
  wf:
    jobs:
      - sigh
