version: 2.1

jobs:
  testcache:
    docker:
      - image: circleci/python:3.7
      - image: circleci/python:3.7
        command: ls -l
      - image: circleci/python:3.7
        entrypoint: pwd
    steps:
      - run: python --version

workflows:
  default-ci-workflow:
    jobs:
      - testcache
