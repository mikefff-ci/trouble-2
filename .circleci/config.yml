version: 2.1

jobs:
  testing:
    machine:
      docker_layer_caching: true
      image: ubuntu-1604:202007-01
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run: docker volume ls
      - run: docker volume ls

workflows:
  wf:
    jobs:
      - testing
