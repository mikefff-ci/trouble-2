version: 2.1

jobs:
  main:
    docker:
      - image: circleci/python:3.7
      - image: circleci/$COMMAND
        command: 'env'
        environment:
          COMMAND1: $COMMAND
    steps:
      - run: echo 123

workflows:
  workflow:
    jobs:
      - main
