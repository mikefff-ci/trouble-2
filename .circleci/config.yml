version: 2.1

jobs: # a basic unit of work in a run
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker:
          version: 19.03.12
      - run: docker build .
      - run: echo 123 > new_context.txt
      - run: docker build .

workflows:
  build:
    jobs:
      - test
