version: 2.1

jobs:
  test:
    docker:
      - image: circleci/php:7.4
    steps:
      - checkout
      - run: |
          composer install
          phpunit --autoload autoload.php phptests
      - run: |
          (
            exit 1;
          ) || {
            if [ $? -ne 0 ]; then
              echo 123;
            fi
          }
workflows:
  wf:
    jobs:
      - test:
          name: with/slash
