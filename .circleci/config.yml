version: 2.1

jobs:
  sigh:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: |
          source source.sh
          testing

workflows:
  wf:
    jobs:
      - sigh
