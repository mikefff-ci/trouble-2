version: 2.1

jobs:
  testing:
    machine:
      docker_layer_caching: true
      image: 'ubuntu-1604:201903-01'
    steps:
      - checkout
      - run: echo "test"
      - run: docker image ls
      - run: docker build .
      - run: docker image ls

workflows:
  wf:
    jobs:
      - testing
