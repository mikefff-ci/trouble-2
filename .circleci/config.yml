version: 2.1

orbs:
  end-of-the-jobs: mkly/end-of-the-jobs@dev:0.2.0

jobs:
  stuff:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "this is invert <<^ pipeline.parameters.testingfalse >> INVERT <</ pipeline.parameters.testingfalse >>"
      - run: echo "this is regular <<# pipeline.parameters.testingtrue >> HERE <</ pipeline.parameters.testingtrue >>"
  morestuff:
    docker:
      - image: cimg/base:stable
    steps:
      - run: sleep 12

workflows:
  wf:
    jobs:
      - end-of-the-jobs/execute_on_end_workflow:
          on_complete_steps:
            - run: echo 123
            - run: echo "done"
      - morestuff
      - stuff
