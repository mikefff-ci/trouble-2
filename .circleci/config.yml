version: 2.1

jobs:
  comment:
    docker:
      - image: cimg/base:stable
    steps:
      - run: |
          echo "above"
          <<! We can also do comments >>
          echo "below"
          environment:
            SOME_ENV: <<! another comment >>Testing

workflows:
  comments:
    jobs:
      - comment
