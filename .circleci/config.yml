version: 2.1

workflows:
  version: 2
  filtered_workflow:
    when: << pipeline.parameters.run_workflow >>
    jobs:
      - my_job
  another_filtered_workflow:
    when: << pipeline.parameters.run_workflow >>
    jobs:
      - my_job
  noop:
    jobs:
      - noop

jobs:
  my_job:
    working_directory: /home/circleci/project/testing/directory
    docker:
      - image: cimg/base:stable
    steps:
      - run: ls /home/circleci/project/testing/directory
