version: 2.1

jobs:
  vm:
    docker:
      - image: cimg/base:stable
    resource_class: small
    steps:
      - checkout
      - setup_remote_docker:
          experimental: true
          version: 19.03.12
      - run: DOCKER_BUILDKIT=1 docker build .
workflows:
  wf:
    jobs:
      - vm
