version: 2.1

jobs:
  trap:
    docker:
      - image: quay.io/pantheon-public/build-tools-ci:1.x
    steps:
      - run:
          command: |
            whoami
            apt-get update && apt-get install -y procps
            while true; do
              sleep 5
              ps auxwf
              echo "======"
            done
      - run: sleep 60

workflows:
  wf:
    jobs:
      - trap
