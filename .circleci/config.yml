version: 2.1

workflows:
  testing:
    jobs:
      - testing

jobs:
  testing:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run: git pull --all
      - run: git tag
      - run: git branch
