version: 2.1

jobs:
  trap:
    docker:
      - image: quay.io/pantheon-public/build-tools-ci:1.x
    steps:
      - run:
          command: |
            apk add ps
            while true; do
              sleep 5
              ps auxwf
              echo "======"
            done
      - run: sleep 60

workflows:
  wf:
    jobs:
      - trap
