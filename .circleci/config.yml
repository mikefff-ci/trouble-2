version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
      - image: adobe/s3mock:latest

    steps:
      - run: |
          while ! nc -z 127.0.0.1 9090 ; do
            sleep 1
          done

          while ! nc -z 127.0.0.1 9191 ; do
            sleep 1
          done

          netstat -plnt

workflows:
  version: 2
  test:
    jobs:
      - test
