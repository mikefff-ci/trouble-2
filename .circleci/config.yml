version: 2.1

jobs:
  main:
    docker:
      - image: cimg/base
    steps:
      - run: echo $(($TEST_ONE == "testing"))
  forked:
    docker:
      - image: cimg/base
    steps:
      - run: echo "forked"

workflows:
  workflow:
    jobs:
      - main:
          context: org-test
          filters:
            branch:
              only:
                - master
                - /internal\/.*/
      - forked
