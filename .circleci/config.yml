version: 2.1

jobs:
  trap:
    docker:
      - image: circleci/python:3.7
    steps:
      - run: echo 123

workflows:
  wf:
    jobs:
      - trap:
          filters:
            tags:
              only: /.*/
            branches:
              only: /.*/
