version: 2.1

jobs:
  python:
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - add_ssh_keys
      - run: touch $BASH_ENV
      - run: cat $BASH_ENV
workflows:
  comments:
    jobs:
      - python
