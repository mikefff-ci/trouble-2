version: 2.1

jobs:
  vm:
    docker:
      - image: mkly/curl-gnutls
        auth:
          username: testing
          password: ""
    steps:
      - run: echo "test"
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "123"

workflows:
  wf:
    jobs:
      - test:
          filters:
            tags:
              only:
                - /.*/
            branches:
              only:
                - /.*/
      - vm:
          filters:
            tags:
              only:
                - /.*/
            branches:
              ignore:
                - /.*/
          requires:
            - test
