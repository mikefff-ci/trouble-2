version: 2.1

parameters:
  testing:
    type: boolean
    default: false

jobs:
  stuff:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "this is invert <<^ pipeline.parameters >> INVERT <</ pipeline.parameters.testing >>"
      - run: echo "this is regular <<# pipeline.parameters >> INVERT <</ pipeline.parameters.testing >>"

workflows:
  wf:
    jobs:
      - stuff
