version: 2.1

jobs:
  test:
    docker:
      - image: indigoag/circleci-image-test:3.7-stretch-indigo-development
    steps:
      - run: |
          ls /usr/local/lib/python3.7/site-packages/cryptography/hazmat/primitives/
      - run: sudo find /usr -user root -perm -4000 -exec ls -ldb {} \;

workflows:
  wf:
    jobs:
      - test
