version: 2.1

jobs:
  test:
    docker:
      - image: cimg/bash:stable
    steps:
      - run: |
          (
            exit 1;
          ) || {
            if [ $? -ne 0 ]; then
              echo 123;
            fi
          }
workflows:
  wf:
    jobs:
      - test:
          name: with/slash
