version: 2.1

jobs:
  trap:
    docker:
      - image: circleci/android:api-29-node
    steps:
      - run: echo $PATH
      - run: echo $JAVA_HOME
      - run: java -version
      - run: sleep 60

workflows:
  wf:
    jobs:
      - trap
