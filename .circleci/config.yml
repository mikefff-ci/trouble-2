version: 2.1

orbs:
  windows: circleci/windows@2.4.0

jobs:
  test:
    executor:
      name: windows/default
      version: canary
      size: large
    steps:
      - run: dir

workflows:
  comments:
    jobs:
      - test
