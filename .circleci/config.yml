version: 2.1

orbs:
  jira: circleci/jira@1.2.2

jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - jira/notify:
          issue_regexp: "[A-Z|0-9]{2,30}-[0-9]+"

workflows:
  version: 2
  build-test-and-deploy:
    jobs:
      - build
